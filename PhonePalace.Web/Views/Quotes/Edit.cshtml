@model PhonePalace.Web.ViewModels.QuoteEditViewModel

@{
    ViewData["Title"] = "Editar Cotización #" + Model.QuoteID;
}

<h1>@ViewData["Title"]</h1>
<hr />

<form asp-action="Edit">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <input type="hidden" asp-for="QuoteID" />

    <div class="row">
        <div class="col-md-3 mb-3">
            <label asp-for="ClientID" class="form-label"></label>
            <select asp-for="ClientID" class="form-select" asp-items="Model.Clients">
                <option value="">-- Seleccione un Cliente --</option>
            </select>
            <span asp-validation-for="ClientID" class="text-danger"></span>
        </div>
        <div class="col-md-3 mb-3">
            <label asp-for="QuoteDate" class="form-label"></label>
            <input asp-for="QuoteDate" class="form-control" />
            <span asp-validation-for="QuoteDate" class="text-danger"></span>
        </div>
        <div class="col-md-3 mb-3">
            <label asp-for="ExpirationDate" class="form-label"></label>
            <input asp-for="ExpirationDate" class="form-control" />
            <span asp-validation-for="ExpirationDate" class="text-danger"></span>
        </div>
        <div class="col-md-3 mb-3">
            <label asp-for="Status" class="form-label"></label>
            <select asp-for="Status" class="form-select" asp-items="Model.Statuses"></select>
            <span asp-validation-for="Status" class="text-danger"></span>
        </div>
    </div>

    <h4 class="mt-4">Detalles de la Cotización</h4>
    <hr />

    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Producto</th>
                <th style="width: 100px;">Cantidad</th>
                <th style="width: 150px;">Precio Unit.</th>
                <th style="width: 150px;">Subtotal</th>
                <th style="width: 50px;"></th>
            </tr>
        </thead>
        <tbody id="quote-details-body">
            @for (int i = 0; i < Model.Details.Count; i++)
            {
                <tr>
                    <td>
                        <select asp-for="Details[i].ProductID" class="form-select product-select" asp-items="Model.Products">
                            <option value="">-- Seleccione Producto --</option>
                        </select>
                    </td>
                    <td>
                        <input asp-for="Details[i].Quantity" type="number" min="1" class="form-control quantity-input" />
                    </td>
                    <td>
                        <input asp-for="Details[i].UnitPrice" type="number" step="0.01" class="form-control price-input" />
                    </td>
                    <td>
                        <input type="text" class="form-control subtotal-display" value="@(Model.Details[i].Quantity * Model.Details[i].UnitPrice)" readonly />
                    </td>
                    <td>
                        <button type="button" class="btn btn-danger btn-sm delete-row-btn">
                            <i class="bi bi-trash"></i>
                        </button>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <button type="button" id="add-product-btn" class="btn btn-success mb-3">
        <i class="bi bi-plus-circle"></i> Añadir Producto
    </button>

    <div class="form-group mt-4">
        <input type="submit" value="Guardar Cambios" class="btn btn-primary" />
        <a asp-action="Details" asp-route-id="@Model.QuoteID" class="btn btn-secondary">Cancelar</a>
    </div>
</form>

<!-- Plantilla para nuevas filas de detalles (oculta) -->
<table class="d-none">
    <tbody id="detail-row-template">
        <tr>
            <td>
                <select class="form-select product-select" asp-items="Model.Products">
                    <option value="">-- Seleccione Producto --</option>
                </select>
            </td>
            <td>
                <input type="number" value="1" min="1" class="form-control quantity-input" />
            </td>
            <td>
                <input type="number" step="0.01" class="form-control price-input" />
            </td>
            <td>
                <input type="text" class="form-control subtotal-display" readonly />
            </td>
            <td>
                <button type="button" class="btn btn-danger btn-sm delete-row-btn">
                    <i class="bi bi-trash"></i>
                </button>
            </td>
        </tr>
    </tbody>
</table>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const addProductBtn = document.getElementById('add-product-btn');
            const detailsBody = document.getElementById('quote-details-body');
            const template = document.getElementById('detail-row-template').querySelector('tr');

            // Attach listeners to existing rows
            detailsBody.querySelectorAll('tr').forEach(row => {
                attachRowEventListeners(row);
            });

            addProductBtn.addEventListener('click', addProductRow);

            function addProductRow() {
                const newRow = template.cloneNode(true);
                detailsBody.appendChild(newRow);
                reindexRows();
                attachRowEventListeners(newRow);
            }

            function reindexRows() {
                const rows = detailsBody.querySelectorAll('tr');
                rows.forEach((row, index) => {
                    row.querySelectorAll('input, select').forEach(input => {
                        if (input.name) {
                            input.name = input.name.replace(/Details\[\d+\]/, `Details[${index}]`);
                        }
                    });
                });
            }

            function attachRowEventListeners(row) {
                row.querySelector('.delete-row-btn').addEventListener('click', function () {
                    row.remove();
                    reindexRows();
                });

                const productSelect = row.querySelector('.product-select');
                const priceInput = row.querySelector('.price-input');
                const quantityInput = row.querySelector('.quantity-input');

                productSelect.addEventListener('change', function () {
                    const productId = this.value;
                    if (productId) {
                        fetch(`@Url.Action("GetProductPrice", "Quotes")?productId=${productId}`)
                            .then(response => response.json())
                            .then(data => {
                                priceInput.value = data.price.toFixed(2);
                                updateSubtotal(row);
                            })
                            .catch(error => console.error('Error al obtener el precio:', error));
                    } else {
                        priceInput.value = '';
                        updateSubtotal(row);
                    }
                });

                quantityInput.addEventListener('input', () => updateSubtotal(row));
                priceInput.addEventListener('input', () => updateSubtotal(row));
            }

            function updateSubtotal(row) {
                const quantity = parseFloat(row.querySelector('.quantity-input').value) || 0;
                const price = parseFloat(row.querySelector('.price-input').value) || 0;
                const subtotal = quantity * price;
                row.querySelector('.subtotal-display').value = subtotal.toFixed(2);
            }
        });
    </script>
}
